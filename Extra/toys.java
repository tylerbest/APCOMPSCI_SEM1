import java.util.Scanner;
import java.util.ArrayList;
public class toys
{
	public static void main(Strin[]args)
	{
		ArrayList<Toys> inventory = new ArrayList<Toys>();
		//inventory.add
		
		
		Scanner kb = new Scanner(System.in);
		System.out.println("Enter your number of Toys");
		String count = kb.nextLine();
		Scanner scan = new Scanner(count);
		
		while(scan.hasNext())
		{
			String x = scan.next();
			for(count : inventory)
			{
				if(b.scan()equals(x) || b.getName().equals(x))
				{
					
				}
			}
		}
	}
}